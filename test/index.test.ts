// import test from 'blue-tape'

// import { parseUrl } from '../src'
// import cases from './cases'

// test('cases', async t => {
//   cases.forEach((c, caseIndex) => {
//     if (c.url) {
//       const result = parseUrl(c.url)
//       t.deepEqual(result, c.result, `${c.name} (${caseIndex + 1})`)
//     } else if (c.urls) {
//       c.urls.forEach((url, urlIndex) => {
//         const result = parseUrl(url)
//         t.deepEqual(
//           result,
//           c.result,
//           `${c.name} (${caseIndex + 1}.${urlIndex + 1})`
//         )
//       })
//     }
//   })
// })
